{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "migrate:gen": "drizzle-kit generate --config ./drizzle.config.mjs --out migrations",
    "migrate:push": "drizzle-kit push --config ./drizzle.config.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "pg": "^8.10.0",
    "dotenv": "^17.0.0",
    "drizzle-orm": "^0.44.2",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.8",
    "drizzle-kit": "^0.31.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "drizzle": {
   "schema": "./src/schema.ts",
   "out": "./drizzle.config.mjs",
   "driver": "pg",
   "dbCredentials": {
     "connectionString": "${DATABASE_URL}"
   }
 }
}
